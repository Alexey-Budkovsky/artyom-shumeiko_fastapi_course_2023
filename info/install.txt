-- установить всю экосистему FastAPI
pip install fastapi[all]

-- requirements.txt
pip freeze > requirements.txt

-- библиотеки для авторизации, аутентификации: passlib, python-jose
pip install passlib python-jose